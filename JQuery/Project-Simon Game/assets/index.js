function simonPick() {
    let rand = Math.ceil(Math.random() * 4);

    if (rand === 1) {
        
    };
    $('.square').on('click', function(event) {
        console.log(event);
    });
};

simonPick();